spring.application.name=Project

# SERVER CONFIG
server.port=8080

# DATABASE CONFIG - MYSQL
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_management?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=tung100705

# JPA / HIBERNATE CONFIG
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT CONFIG
jwt.secret=THIS_IS_A_SECRET_KEY_FOR_JWT_MUST_BE_AT_LEAST_32_CHARS
jwt.access-token-expiration-ms=1800000
jwt.refresh-token-expiration-ms=604800000

# FILE UPLOAD CONFIG
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CLOUDINARY CONFIG
cloudinary.cloud-name=your_cloud_name
cloudinary.api-key=your_api_key
cloudinary.api-secret=your_api_secret
